package Lesson_1.Marathon;

public class Human extends Creature {


    public Human(String type, String name, int maxRunDistance, int runSpeed, int maxSwimDistance, int swimSpeed, int maxJumpHeight, boolean onDistance) {
        super(type, name, maxRunDistance, runSpeed, maxSwimDistance, swimSpeed, maxJumpHeight, onDistance);
    }
}
