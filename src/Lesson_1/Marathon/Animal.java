package Lesson_1.Marathon;

public class Animal extends Creature {


    public Animal(String type, String name, int runSpeed, int swimSpeed, int maxJumpHeight) {
        super(type, name, runSpeed, swimSpeed, maxJumpHeight);
    }
}
